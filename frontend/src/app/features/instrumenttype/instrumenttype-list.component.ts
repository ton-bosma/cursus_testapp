import { ChangeDetectionStrategy, Component } from '@angular/core';

/**
 * Placeholder component for the Instrumenttype feature.
 * Implementation is provided by a later ticket.
 */
@Component({
  selector: 'app-instrumenttype-list',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `<p>Types — wordt ingevuld in een later ticket.</p>`,
})
export class InstrumenttypeListComponent {}
